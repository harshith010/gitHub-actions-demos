name: output_demo
on: push
jobs:
  outputDemo:
    runs-on: ubuntu-latest
    steps:
      - name: step 1
        id: one
        run: echo "version=0.01" >> $GITHUB_OUTPUT
      - name: step 2
        run: echo "this version is ${{ steps.one.version }}"